import { StyleSheet, Platform } from "react-native";
import Constants from "expo-constants";
import { screenHeight } from "../../commons";

export default StyleSheet.create({
  root: {
    paddingBottom: screenHeight * 0.1,
  },
});
