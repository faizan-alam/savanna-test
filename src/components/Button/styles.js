import { StyleSheet } from "react-native";

export default StyleSheet.create({
  root: {
    padding: 15,
    borderRadius: 10,
    backgroundColor: "#422adf",
  },
  disabled: {
    backgroundColor: "#422adf",
    opacity: 0.8,
  },
});
